# 党务服务中心数字化平台 — 产品功能规格说明书

| 项目 | 说明 |
|------|------|
| 文档版本 | **V1.3** |
| 状态 | 需求基线（可立项 / 可拆迭代） |
| 主场景 | 街道党群服务中心、机关党委（可扩展社区 / 楼宇 / 两新） |
| 交付形态 | **SaaS：标准版多租户 + 大客户独立部署**（一套代码，两种部署） |
| 技术基线 | 等保二级及以上；主库优先 PostgreSQL |
| 端形态 | 党员端 / 群众端（小程序·App·H5）、党务工作台、租户管理后台、**平台运营后台**、数据驾驶舱 |

---

## 1. 文档说明

### 1.1 目的

明确「党务服务中心」一体化数字化平台的产品定位、**SaaS 多租户与独立部署策略**、角色、功能范围、优先级、非功能要求及接口预留，作为产品设计、研发排期、测试验收与后续扩展的统一依据。

### 1.2 范围边界（已确认）

| 序号 | 议题 | 结论 |
|------|------|------|
| 1 | 视觉专注度（学习场景） | **可选增强**；组织可开关；默认关闭；原始视频不上传 |
| 2 | 人脸能力 | **全场景可配置**（签到 / 考试 / 登录 / 大厅等独立开关） |
| 3 | 首要场景 | 街道党群服务中心 + 机关党委 |
| 4 | AI 策略 | **本机 + 云端统一接口**；首期规划双轨，最终选型后置 |
| 5 | 安全与数据库 | 尽量等保二级以上；主库 **PostgreSQL** |
| 6 | 网格事件 | **平台自建闭环**，并 **对外开放 API** |
| 7 | 党费在线支付 | **本期不做**；**必须预留支付与对账接口** |
| 8 | 群众端 | 党务公开与指引 + 活动 + 诉求 + **办事** |
| 9 | 组织与权限 | 支持多组织架构、细粒度权限与功能包配置 |
| 10 | **交付形态** | **标准版多租户 SaaS + 大客户独立部署**；一套代码，租户为第一公民 |

### 1.3 优先级定义

| 级别 | 含义 |
|------|------|
| **P0** | 首期必做，无则无法交付主场景 / SaaS 底座 |
| **P1** | 首期强烈建议，可与 P0 同迭代或紧随其后 |
| **P2** | 二期及以后；架构需预留，避免推倒重来 |
| **预留** | 本期不实现业务，但必须定义接口 / 表结构 / 开关，禁止写死 |

### 1.4 端与模块代号

| 代号 | 端 / 层 |
|------|---------|
| MP | 移动端（微信小程序 / App / H5） |
| WT | 党务工作台（移动 + Web） |
| ADM | **租户**管理后台（Web） |
| OPS | **平台运营后台**（仅平台方，非租户可见） |
| BI | 数据驾驶舱 / 大屏 |
| AI | AI 能力中台（Gateway） |
| OPEN | 开放平台（对外 API） |

---

## 2. 产品定位与目标

### 2.1 一句话定位

面向 **街道党群服务中心** 与 **机关党委** 的 **SaaS 化** 多组织、多权限一体化平台：标准客户走多租户云服务，政务与大客户可独立部署；覆盖党员管理、组织生活、学习教育、党群服务、办事与诉求、网格事件；支持本地与云端 AI 统一调用；安全可对标等保二级；党费支付本期不做但接口预留。

### 2.2 建设目标

1. **党务规范化**：三会一课、发展党员、党费台账、转接等留痕可督。
2. **服务一体化**：阵地、活动、办事、诉求、志愿在同一入口。
3. **学习可核验**：文件 / 视频学习行为可统计；专注度可选增强。
4. **治理可协同**：网格事件自建闭环，并可被外部系统对接。
5. **智能可演进**：AI 双轨架构首期落地，模型与部署方式可后期切换。
6. **权限可配置**：不同组织类型、不同角色看到不同功能与数据范围。
7. **SaaS 可运营**：**一套代码、两种部署**；租户开通、配额、隔离、升级可标准化交付。

### 2.3 非目标（本期明确不做）

- 党费 **在线支付扣款**（微信 / 支付宝等）——仅预留接口与数据模型。
- 替代城管执法、应急指挥等完整「大城运」系统——网格以党建党群侧事件为主，可对接外部。
- 强制全员开启摄像头专注度或人脸登录。
- 绑定单一云厂商大模型或单一本地模型品牌。
- 仅支持公有多租户、不支持专有部署（或反之）——**两种部署均为正式产品形态**。

### 2.4 产品口令（研发与商务统一）

> **一套代码、两种部署；租户是第一公民；组织是租户内树。**

---

## 3. SaaS 与部署架构（V1.1 核心新增）

### 3.1 双模式定义

| 模式 | 名称 | 适用客户 | 资源形态 | 隔离级别 |
|------|------|----------|----------|----------|
| **Mode-S** | 标准版多租户 SaaS | 中小街道试点、企业党组织、两新、商协会、快速开户客户 | 共享应用集群 + 共享 PostgreSQL（逻辑隔离） | 行级 `tenant_id` 强隔离 |
| **Mode-D** | 大客户独立部署 | 区县/街道政务云、机关专网、对数据主权与等保责任要求高的客户 | 独立应用实例 + 独立数据库（或独立 Schema/库） | 实例级 / 库级隔离 |

**统一要求**：

- 同一套制品（镜像 / 安装包）、同一套业务功能、同一套配置模型。
- 通过部署参数切换：`DEPLOY_MODE=shared|dedicated`。
- 独立部署实例在逻辑上仍是一个（或少数）租户，便于日后回收进统一运营或升级。

### 3.2 层级模型

```text
平台运营方（Platform）
  └── 租户 Tenant                    ← SaaS 计费、开通、配额、功能包的单位
        └── 组织树 Org               ← 党委 / 党工委 / 支部 / 网格等
              └── 用户 User / 角色 / 业务数据
```

| 概念 | 说明 |
|------|------|
| **平台** | 软件提供方；拥有 OPS 后台；管理所有标准版租户与独立实例登记 |
| **租户** | 一个签约主体（某街道党工委、某机关党委、某集团党委等） |
| **组织** | 租户内部党组织树节点；业务权限与数据范围作用在组织上 |
| **用户** | 可属于一个租户；通过组织成员关系获得数据范围 |

**禁止混淆**：不能仅用「组织」模拟租户；所有业务表必须具备租户上下文。

### 3.3 标准版多租户（Mode-S）

#### 3.3.1 隔离方案（默认）

- 共享应用、共享库表。
- **所有业务表含 `tenant_id`**（UUID 或雪花 ID），非空。
- 数据访问层强制注入租户条件（拦截器 / 行级策略），禁止只靠业务开发「记得写 where」。
- 缓存 Key、对象存储路径、消息队列主题分区均带租户维度。
- 开放 API 的 AppId **绑定唯一租户**。

#### 3.3.2 租户能力

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 租户档案 | P0 | OPS | 名称、编码、类型、地区、联系人、状态 |
| 租户状态机 | P0 | OPS | 试用 → 正式 → 冻结 → 关闭 |
| 场景包 | P0 | OPS | 街道党群包 / 机关党委包（默认菜单与模块） |
| 功能开关 | P0 | OPS/ADM | 模块级；OPS 控制上限，ADM 在授权范围内再开 |
| 配额 | P0 | OPS | 党员数、存储、AI 调用、开放 API 频次、管理员数等 |
| 开通向导 | P0 | OPS | 创建租户 → 场景包 → 根组织 → 首个管理员 → 完成 |
| 品牌（基础） | P1 | OPS/ADM | 系统名称、Logo；标准版可用子域 `tenant-code.platform.com` |
| 数据导出 | P1 | OPS/ADM | 租户申请导出；关闭前强制导出策略 |
| 销毁 / 匿名化 | P1 | OPS | 关闭后保留期、硬删除流程与审计 |

#### 3.3.3 登录与入口（标准版）

- 统一登录页：账号选择租户，或 **单位码 / 邀请码** 进入指定租户。
- 小程序：同一小程序多租户（登录后绑定租户）为默认；大客户可再配独立小程序 AppId（见独立部署）。
- 用户手机号在不同租户可分别存在（以 `tenant_id + mobile` 唯一）。

### 3.4 大客户独立部署（Mode-D）

#### 3.4.1 形态

- 独立 VPC / 政务云项目 / 客户机房。
- 独立 PostgreSQL（推荐）、独立对象存储桶、独立密钥。
- 可关闭平台「多租户开通」能力，实例内通常 **单租户或少量租户**（如区级平台下挂多个街道作为组织，或少数租户）。
- 支持客户侧域名、Logo、名称（白标）。
- 可选：与客户 IdP / 统一身份对接（P2）。

#### 3.4.2 与标准版的差异（配置级，非两套代码）

| 项 | 标准版 Mode-S | 独立部署 Mode-D |
|----|---------------|-----------------|
| 数据库 | 共享库 + tenant_id | 独立库（或独立实例） |
| OPS | 平台集中运营 | 可裁剪为「实例运维台」或保留精简 OPS |
| 升级 | 平台滚动升级 | 约定窗口、客户环境升级包 |
| 小程序 | 平台统一小程序或多租户 | 常为客户独立 AppId |
| 等保责任 | 平台主体 + 租户约定 | 多为客户主体，厂商配合 |
| AI | 默认云端 + 可选本地 | 常强制 local_only 或客户内网模型 |
| 出网 | 按租户策略 | 常物理隔离，严格出域审批 |

#### 3.4.3 交付物（独立部署）

- 安装包 / 容器编排清单（Helm / compose 等）
- 配置模板（`DEPLOY_MODE=dedicated`）
- 初始化脚本（建库、首租户、管理员）
- 备份恢复与监控接入说明
- 版本升级手册

### 3.5 平台运营后台（OPS）

仅平台运营人员使用；**租户管理员不可见**。

| 功能点 | 优先级 | 描述 |
|--------|--------|------|
| 租户列表与检索 | P0 | 状态、套餐、配额使用率 |
| 开通 / 冻结 / 关闭 | P0 | 含原因与操作审计 |
| 套餐与功能包定义 | P0 | 模块组合、默认配额 |
| 全局监控 | P1 | 错误率、慢查询、AI 费用、存储 |
| 工单 / 公告（对租户） | P2 | 运维通知 |
| 独立实例登记台账 | P1 | 大客户实例版本、环境、联系人（即使数据不在共享库） |
| 跨租户数据访问 | — | **原则上禁止**；应急需双人授权 + 全程审计（P2 制度） |

### 3.6 租户管理后台（ADM）与组织权限

- ADM 仅操作 **当前租户** 数据。
- 租户内：RBAC + 组织数据范围（本级 / 下级 / 本网格）。
- 场景包决定默认菜单；租户管理员可在授权模块内再细分角色。

### 3.7 套餐与配额（模型首期具备，计费规则可简）

| 配额项 | 说明 | 超限策略 |
|--------|------|----------|
| max_members | 党员/用户规模 | 禁止新增并提示 |
| max_storage_gb | 文件与影像 | 禁止上传 |
| max_ai_calls_month | AI 调用 | 降级或拒绝（可配） |
| max_open_api_qps | 开放 API | 限流 |
| modules[] | 已开通模块列表 | 菜单与 API 双禁 |

计费对接（发票、自动续费）为 **P2**；首期支持人工改套餐与到期日即可。

### 3.8 技术约束（研发强制）

1. 中间件 / ORM 拦截：无 `tenant_id` 的业务查询不得发布到生产。
2. 定时任务按租户分片或携带租户上下文。
3. 文件路径：`/{tenant_id}/...`。
4. 审计日志含 `tenant_id`、`operator_id`、`request_id`。
5. 自动化测试：**跨租户越权** 为发布门禁用例。
6. 独立部署与标准版使用同一迁移脚本；禁止「独立版私自改表」。

### 3.9 SaaS 相关验收要点

1. 创建租户 A、B 后，A 的管理员无法读写 B 的任何业务数据（含导出、搜索、开放 API）。
2. 关闭某模块后，ADM 菜单与对应 API 均不可用。
3. 配额用尽后行为符合配置（拒绝而非写脏数据）。
4. 同一制品可在 `shared` 与 `dedicated` 配置下完成部署冒烟。
5. 开通向导 15 分钟内可完成「可登录的空租户 + 根组织 + 管理员」。

### 3.10 技术栈与架构选型（V1.3 新增）

#### 3.10.1 后端：全栈 Rust

| 层级 | 技术 | 说明 |
|------|------|------|
| Web 框架 | Axum（tokio 生态） | 高性能异步路由；中间件生态成熟 |
| ORM / 数据库 | SeaORM 或 SQLx | SQLx 编译时检查 SQL；SeaORM 提供实体关系映射 |
| 缓存 | deadpool-redis | 连接池管理 Redis |
| 任务队列 | apalis | 基于 PostgreSQL 或 Redis 的后台任务 |
| 认证 | jsonwebtoken + argon2 | JWT 签发验证 + 密码安全哈希 |
| API 文档 | utoipa | 自动生成 OpenAPI 3.0 文档 |
| WebSocket | axum 内置 | 实时通知、协同编辑 |
| WASM 沙箱 | wasmtime | 安全执行用户自定义规则和插件 |
| 序列化 | serde + serde_json | Rust 生态标准序列化框架 |
| 日志 | tracing + tracing-subscriber | 结构化日志、链路追踪 |

**Rust 选型理由：**
- 单二进制交付，Mode-D 独立部署仅需一个可执行文件 + 配置文件。
- 工作流引擎的事件溯源和状态机天然适合 Rust 类型系统。
- AI Gateway 低延迟高吞吐场景有明确性能优势。
- 内存安全保证在政务等保场景下降低安全风险。

#### 3.10.2 WASM 应用场景

| 场景 | 说明 | 优先级 |
|------|------|--------|
| 规则沙箱执行 | ADM 后台编写的审批条件表达式编译为 WASM 模块 → wasmtime 安全执行 | P1 |
| 插件系统 | 功能模块以 WASM 插件形式动态加载卸载，实现 SaaS 场景包按需启用 | P1 |
| 自定义表单校验 | 租户管理员定义的表单验证逻辑编译为 WASM → 前后端共享同一套校验 | P2 |
| 数据转换管道 | Excel 导入字段映射和清洗规则 → 可插拔 WASM 模块 | P2 |

#### 3.10.3 移动端：uni-app x

- 基于开源项目 [Cool Unix](https://unix.cool-js.com/) 二次开发。
- 集成 SDUI（Server-Driven UI）动态渲染引擎，支持管理端可视化配置页面布局，详见 TA-08。

| 层级 | 技术 | 说明 |
|------|------|------|
| 项目基座 | **Cool Unix**（cool-team-official/cool-unix） | 基于 uni-app x 的开源跨端脚手架；与 cool-admin 同生态；MIT 协议免费商用 |
| 跨端框架 | uni-app x | DCloud 新一代方案；UTS 编译为原生 Kotlin/Swift 渲染，无 WebView 无 JS 引擎 |
| 构建工具 | Vite | 极速开发体验 |
| CSS 方案 | Tailwind CSS | 实用优先原子化 CSS；配合多主题和暗黑模式 |
| 内置组件库 | cool-ui（uvue） | 80+ 原生级别组件；涵盖表单、列表、布局、数据展示、反馈等全部类型 |

**Cool Unix 核心能力（开箱即用）：**

| 能力 | 包含内容 |
|------|----------|
| 基础设施 | Router 路由 / Pinia Store / Service 统一请求层 / Hooks 组合函数 / Ctx 上下文 |
| 表单组件 | Form 表单验证、Input、Textarea、Radio、Checkbox、Switch、Select、Cascader、Calendar、Upload |
| 数据展示 | List、ListViewRefresh 下拉刷新、Waterfall 瀑布流、Banner、Timeline、Tree、Pagination |
| 反馈交互 | ActionSheet、Popup、Confirm、Toast、Skeleton、Noticebar |
| 特色功能 | Qrcode 扫码、Sign 手写签名、SlideVerify 滑动验证、Cropper 图片裁剪、Watermark 水印 |
| 登录体系 | 微信小程序登录 / App 微信授权 / 运营商一键登录 / 短信验证码 |
| 主题系统 | 多主题切换 / 暗黑模式 / 品牌色定制（适配政务视觉规范） |
| 国际化 | i18n 多语言支持 |
| uni_modules | cool-canvas、cool-share 分享、cool-svg 图标、cool-open-web 内嵌浏览器 |

**选型理由：**
- 完全开源免费（MIT），无需商业授权费用。
- 与 cool-admin 同团队同生态，前后端设计理念一致，降低协作成本。
- 提供完整项目脚手架（路由/状态/请求/登录/i18n），不是纯 UI 组件库，可大幅减少基础搭建工作。
- 原生渲染性能优于 Webview 方案，适合对体验有要求的 App 场景。
- 支持鸿蒙 HarmonyOS，未来可覆盖政务信创需求。
- 视觉设计达到商业 App 水准，非「后台管理系统风格」。
- 提供丰富的页面模板和布局方案，可快速搭建高颜值界面。
- 支持 uni-app x（uvue）原生渲染，性能优于 Webview 方案。
- 有免费基础版可用，付费版提供更多高级组件和模板。

#### 3.10.4 管理端前端：Vue 3 全家桶

| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Vue 3 + TypeScript + Vite | 组合式 API + 类型安全 + 极速 HMR |
| UI 库 | shadcn-vue 或 Naive UI | shadcn-vue：无头组件 + Tailwind CSS，代码可控性最高，AI 生成效果最佳；Naive UI：组件丰富开箱即用 |
| CSS | Tailwind CSS v4 | 原子化 CSS；与 shadcn-vue 配合最佳 |
| 状态管理 | Pinia | 与移动端共享状态管理模式 |
| 图表 | ECharts 5 | 驾驶舱大屏图表渲染 |
| 表格 | TanStack Table (Vue) | 高性能数据表格；支持虚拟滚动、服务端分页排序 |
| 构建 | Vite 6 + Rolldown | 构建速度极快 |

**管理端 AI-friendly 设计原则：**

| 原则 | 说明 |
|------|------|
| 类型驱动开发 | 后端 OpenAPI 自动生成前端 TypeScript 接口类型；前后端类型完全一致 |
| 声明式组件 | 页面通过配置对象描述而非手写模板；LLM 更容易理解和修改 |
| 目录约定 | 统一的文件命名和目录结构规则；AI 编码工具可以精准定位文件 |
| 组件文档化 | 每个公共组件附带 JSDoc 注释和使用示例；AI 可直接引用 |


---

## 4. 用户角色

| 角色 | 说明 | 主要端 |
|------|------|--------|
| 群众 / 居民 | 活动、办事、诉求、公开信息 | MP |
| 普通党员 | 学习、组织生活、党费登记查询、志愿等 | MP |
| 新就业群体 | 驿站服务、暖新活动、简易学习 | MP |
| 入党申请人 / 积极分子等 | 发展流程材料与进度 | MP / WT |
| 网格员 / 社工 | 走访、事件上报、全岗通知识 | WT |
| 支部书记 / 组织委员 | 本支部党务、计划、审核 | WT / ADM |
| 党务工作者 / 组织员 | 台账、督导、材料审核 | WT / ADM |
| 街道党工委管理员 | 区域统筹、考核、联建、督办 | ADM / BI |
| 机关党委管理员 | 机关党建责任、下属支部督导 | ADM / BI |
| 租户系统管理员 | 本租户组织、权限、开关、审计 | ADM |
| **平台运营人员** | 开户、套餐、冻结、全局监控、实例台账 | **OPS** |
| 领导 | 态势、预警、简报 | BI / MP 简报 |

---

## 5. 总体功能架构

```
┌──────────────────────────────────────────────────────────────────┐
│ MP 党员/群众   WT 工作台   ADM 租户后台   BI 驾驶舱   OPS 平台运营 │
└───────────────────────────────┬──────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────┐
│              API 网关 + 统一鉴权 + 租户上下文注入                    │
└───┬───────────┬───────────┬───────────┬───────────┬──────────────┘
    │           │           │           │           │
┌───▼───┐ ┌─────▼─────┐ ┌───▼────┐ ┌────▼────┐ ┌───▼────┐
│ 党务  │ │ 党群服务  │ │ 学习   │ │ 网格事件 │ │ 租户/  │
│ 组织  │ │ 办事诉求  │ │ 教育   │ │ 开放API  │ │ 套餐   │
└───┬───┘ └─────┬─────┘ └───┬────┘ └────┬────┘ └───┬────┘
    │           │           │           │           │
┌───▼───────────▼───────────▼───────────▼───────────▼──────────────┐
│ 主数据 │ 消息 │ 文件 │ 积分 │ 考核督办 │ 配置中心 │ 审计        │
└───────────────────────────┬──────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼────┐        ┌─────▼─────┐       ┌─────▼─────┐
   │ AI Gateway│        │ 人脸/视觉  │       │ 支付适配层 │
   │ 本地/云端 │        │ 可配置引擎 │       │ (仅预留)   │
   └─────────┘        └───────────┘       └───────────┘
                            │
              ┌─────────────┴─────────────┐
              │  Mode-S: 共享 PG + tenant  │
              │  Mode-D: 独立 PG 实例      │
              │  对象存储 / 密钥按模式隔离  │
              └───────────────────────────┘
```

---

## 6. 功能详细说明

说明：每项含 **优先级、端、描述**；所有业务数据默认在 **租户** 范围内；涉及 AI / 人脸 / 支付处单独标注。

---

### 6.1 组织与主数据

#### 6.1.1 党组织管理

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 组织树维护 | P0 | ADM | 多级树：机关党委 / 街道党工委 → 下级党委、总支、支部、党小组等 |
| 组织类型 | P0 | ADM | 机关、街道、社区、国企、两新、楼宇、商圈、园区等可扩展枚举 |
| 组织生命周期 | P1 | ADM | 成立、变更、换届、撤销、合并拆分；材料与批复号留痕 |
| 组织地图 | P2 | ADM/BI | 阵地坐标、覆盖范围、与网格绑定展示 |
| 租户内数据隔离 | P0 | 全端 | 仅本租户；组织间再按数据权限过滤 |
| 上级汇总 | P1 | ADM/BI | 租户内上级组织可查看授权范围内下级汇总 |

**验收要点**：同一标准版集群下租户互不可见；单租户内可配置完整街道或机关组织树。

#### 6.1.2 党员与队伍档案

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 党员档案 | P0 | ADM/WT/MP | 基本信息、入党时间、职务、联系方式、状态 |
| 标签体系 | P1 | ADM | 在职/退休、流动、困难、双报到、网格党员等 |
| 成长轨迹 | P1 | WT/MP | 组织生活、学习、志愿、奖惩、谈话时间线 |
| 流动党员 | P1 | ADM/WT | 流出/流入登记、联系记录 |
| 困难党员 | P1 | ADM/WT | 台账、帮扶、走访；字段脱敏与权限控制 |
| 党务干部名册 | P1 | ADM | 机关侧组织员、书记等名册 |

#### 6.1.3 发展党员

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 阶段流程配置 | P0 | ADM | 申请→积极分子→发展对象→预备→转正；环节可配置 |
| 材料与时限 | P0 | WT/MP | 模板、上传、时限、培养联系人 |
| 会议与公示 | P1 | WT | 与组织生活关联；公示记录 |
| 进度查询 | P0 | MP | 申请人查看本人进度 |
| 超时预警 | P1 | WT/ADM | 超期未推进提醒 |
| 材料完整性检查 | P2 | AI | 规则 + 可选 AI 缺件提示 |

#### 6.1.4 组织关系转接

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 线上申请与审批 | P0 | MP/WT/ADM | 转出/转入、多级审批、电子回执（默认租户内） |
| 转接中锁定 | P1 | 系统 | 转接期间相关策略可配 |
| 跨租户转接 | P2 | OPEN | 预留；需双方租户或平台策略，首期可不实现 |
| 外部系统对接预留 | P2 | OPEN | 预留转接状态交换接口 |

#### 6.1.5 党费管理（支付本期不做）

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 党费标准维护 | P0 | ADM/WT | 按人按月应缴金额；支持导入与人工核定 |
| 应缴账单生成 | P0 | 系统 | 按月生成应缴记录 |
| 缴纳登记（线下） | P0 | WT/ADM | 登记已缴、缴费月份、方式（现金/转账等）、经办人 |
| 代缴登记 | P1 | WT | 授权代缴记录 |
| 党员查询本人记录 | P0 | MP | 已缴至哪月、历史明细、电子回执（登记类） |
| 欠费提醒 | P1 | 消息 | 站内信 / 订阅消息（通道可配） |
| 收缴统计 | P0 | ADM/BI | 收缴率、欠费名单、导出 |
| 党费使用台账 | P2 | ADM | 支出申请与公示（权限严格） |
| **在线支付** | **预留** | — | **本期不实现**；见第 9 章支付适配层 |
| 工资基数自动核算 | P2 | ADM | 对接人事/工资可选；本期非必须 |

**验收要点（党费）**：无第三方支付也可完成「标准 → 账单 → 登记 → 统计」全闭环；代码与表结构不阻断未来接入支付。

---

### 6.2 组织生活与党务工作

#### 6.2.1 三会一课 / 主题党日

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 计划管理 | P0 | WT/ADM | 年度/季度计划、上报审核 |
| 会议创建与通知 | P0 | WT | 类型、时间、地点、参会人、资料包 |
| 请假 | P0 | MP | 请假原因、审批 |
| 签到 | P0 | MP/WT | 扫码；可叠加定位 / 人脸（可配） |
| 会议记录与纪要 | P0 | WT | 模板、附件、归档 |
| 决议转待办 | P1 | WT | 决议事项生成任务与责任人 |
| 统计与督办 | P0 | ADM/BI | 完成率、未开提醒、排名 |
| 视频会议入口 | P2 | MP/WT | 对接第三方会议（链接级即可） |

#### 6.2.2 组织生活会 / 民主生活会

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 方案与材料 | P1 | WT | 对照检查、发言材料 |
| 整改清单销号 | P1 | WT/ADM | 问题、措施、时限、反馈 |

#### 6.2.3 民主评议党员

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 评议方案与等次 | P1 | WT/ADM | 流程、结果登记、公示 |

#### 6.2.4 谈心谈话

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 谈话记录 | P1 | WT | 对象、主题、时间；敏感字段权限 |
| 待跟进事项 | P1 | WT | 与待办打通 |

#### 6.2.5 党务公开

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 栏目与发布 | P0 | ADM/WT | 审批、公示期、已读统计 |
| 意见反馈 | P1 | MP | 对公开内容留言（审核后展示） |

#### 6.2.6 机关党委特色

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 党建责任清单 | P1 | ADM | 处室/支部责任、落实反馈 |
| 党建与业务融合台账 | P1 | ADM | 项目级党建记录 |
| 警示教育 / 作风专栏 | P1 | MP/ADM | 内容发布与学习关联 |
| 下属单位督导检查 | P1 | ADM | 检查清单、问题、整改 |
| 对标评估指标 | P2 | ADM | 如党支部标准化等可配置指标 |

#### 6.2.7 街道党工委 / 区域化党建

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 共建单位库 | P1 | ADM | 驻区单位、联系人 |
| 三张清单 | P1 | ADM | 资源清单、需求清单、项目清单 |
| 联建活动 | P1 | WT/MP | 点单、派单、活动归档 |
| 双报到双报告 | P1 | MP/ADM | 报到、服务记录、时长、评价 |
| 两新 / 新兴领域覆盖台账 | P1 | ADM | 覆盖情况、指导员 |
| 下级调度督办 | P0 | ADM | 任务下发与反馈 |

---

### 6.3 党群服务中心

#### 6.3.1 阵地与空间

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 功能室管理 | P0 | ADM | 会议室、活动室、驿站等 |
| 场地预约 | P0 | MP/WT | 排期、审批、冲突检测 |
| 开放信息展示 | P0 | MP | 开放时间、服务项目 |
| 暖新驿站登记 | P1 | MP/WT | 新就业群体服务记录（可简化） |

#### 6.3.2 活动运营

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 活动发布 | P0 | WT/ADM | 分类、名额、报名起止 |
| 报名 / 候补 | P0 | MP | 取消规则可配 |
| 活动签到 | P0 | MP | 与签到中台复用 |
| 相册与总结 | P1 | WT | 归档展示 |
| 活动统计 | P0 | ADM/BI | 参与人次、热度 |

#### 6.3.3 办事服务（事项引擎）

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 事项配置 | P0 | ADM | 名称、指南、材料、表单、流程、时限、适用组织 |
| 办事指南 | P0 | MP | 条件、材料、流程说明 |
| 在线申报 / 预审 | P0 | MP | 表单提交、材料上传 |
| 办理与进度 | P0 | WT/MP | 受理、补正、办结、进度查询 |
| 评价 | P1 | MP | 办结后评价 |
| 全岗通知识库 | P1 | WT | 政策与口径检索；可接 AI 问答 |

#### 6.3.4 诉求与微心愿

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 诉求上报 | P0 | MP | 分类、描述、附件、位置（可选） |
| 分派办理 | P0 | WT/ADM | 分派、办理、回复 |
| 评价与归档 | P1 | MP/ADM | 满意度 |
| 微心愿 | P1 | MP/WT | 发布、认领、兑现反馈 |

#### 6.3.5 志愿服务与社会力量

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 志愿者注册 | P1 | MP | 与党员/群众身份关联 |
| 志愿项目 | P1 | WT/MP | 报名、签到、时长 |
| 社会组织台账 | P2 | ADM | 入驻、项目（简版） |

---

### 6.4 学习教育中心

#### 6.4.1 内容管理

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 文件学习 | P0 | MP/ADM | PDF/Word/图片；支持 OCR 入库（P1） |
| 视频学习 | P0 | MP/ADM | 课程、进度 |
| 图文 / 专题 / 音频 | P1 | MP/ADM | 多形态内容 |
| 必修选修与学时 | P0 | ADM | 计划、学分规则 |
| 题库与考试 | P0 | MP/ADM | 练习、正式考、成绩归档 |
| 错题本 / 竞赛 | P2 | MP | 增强学习体验 |
| 心得笔记 | P1 | MP | 提交与精选（审核） |

#### 6.4.2 学习行为核验（默认能力）

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 页面/章节停留时长 | P0 | MP | 心跳上报；后台挂起不计或降权（可配） |
| 视频有效观看 | P0 | MP | 进度、拖拽、倍速、切后台规则可配 |
| 防异常刷学 | P1 | 系统 | 多端同登、异常序列标记 |
| 学时认定规则引擎 | P0 | ADM | 组织可配置认定公式 |

#### 6.4.3 视觉专注度（可选增强）

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 组织级 / 租户级开关 | P1 | ADM | 默认关闭；受 OPS 功能包约束 |
| 用户授权 | P1 | MP | 明确告知用途与范围 |
| 端侧/本地推断 | P1 | MP/AI | 朝向屏幕、离开检测 |
| 数据最小化 | P1 | 系统 | **不上传原始视频**；仅时长/特征 |
| 与学时关联 | P2 | ADM | 可选；默认不强制绑定 |
| 无摄像头降级 | P1 | MP | 自动回落停留+播放规则 |

#### 6.4.4 考试增强

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 限时与防切屏 | P1 | MP | 可配置 |
| 考试前人脸核验 | P1 | MP | 可配置；见人脸中台 |

---

### 6.5 签到考勤与身份核验

#### 6.5.1 签到中台

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 扫码签到 | P0 | MP | 动态码 / 静态码策略可配 |
| 定位签到 | P0 | MP | GPS/北斗 + 电子围栏 |
| 围栏与异常 | P1 | ADM | 半径、虚拟定位标记、补签审批 |
| 室内辅助定位 | P2 | MP | Wi-Fi/蓝牙信标（可选） |
| 与业务绑定 | P0 | 系统 | 组织生活、活动、志愿、双报到 |

#### 6.5.2 人脸中台（可配置）

| 场景 | 优先级 | 默认建议 | 说明 |
|------|--------|----------|------|
| 活动/会议签到 | P1 | 可开 | 活体 + 1:1 |
| 考试入场 | P1 | 可开 | 防替考 |
| 登录二次验证 | P1 | 默认关 | 密码/短信为主 |
| 大厅闸机 | P2 | 可开 | 本地引擎优先 |
| 底库采集与更新 | P1 | ADM | 授权、注销、保留期限；数据归属本租户 |
| 本地/云端引擎切换 | P1 | ADM | 敏感环境可强制本地；独立部署常默认本地 |
| 比对审计日志 | P0 | ADM | 谁、何时、场景、结果 |

---

### 6.6 政策文件与知识中枢

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 文件登记入库 | P0 | ADM | 密级、公开范围、承办组织 |
| OCR 与版式 | P1 | AI | 扫描件识别 |
| 智能摘要 / 要点 | P1 | AI | 统一 Gateway 调用 |
| 情境化解读 | P1 | AI | 「对本街道/本机关的影响」类提示词模板 |
| 带出处问答 | P1 | AI/MP/WT | 强制引用片段 |
| 落实清单生成 | P1 | AI/WT | 责任、节点、材料；人工确认后转任务 |
| 知识库检索 | P0 | WT/MP | 党规、文件、办事口径；权限过滤 |
| 向量库 | P1 | AI | 按租户隔离；本地/云端可切换 |

---

### 6.7 网格事件（自建闭环 + 开放）

#### 6.7.1 内部闭环

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 网格树 | P0 | ADM | 街道—社区—网格—微网格等可配 |
| 事件上报 | P0 | WT/MP | 分类、描述、图片、位置 |
| 分派与办理 | P0 | WT/ADM | 流程状态机 |
| 反馈评价 | P1 | MP | 上报人评价 |
| 统计看板 | P0 | BI/ADM | 量、时效、类型分布 |
| 与诉求打通 | P1 | 系统 | 可转为诉求或互相关联 |

#### 6.7.2 对外开放（OPEN）

| 能力 | 优先级 | 说明 |
|------|--------|------|
| 创建事件 | P1 | 外部系统推送入库 |
| 更新状态 / 办结回写 | P1 | 双向协同 |
| 查询事件 | P1 | 按租户与权限过滤 |
| 状态变更回调 | P1 | Webhook |
| 应用鉴权 | P0 | AppId/Secret 绑定租户；签名；IP 白名单 |
| 审计 | P0 | 全部开放调用记日志 |

**事件状态机（建议）**：`草稿 → 已提交 → 已分派 → 处理中 → 已办结 → 已关闭`；支持 `退回补正`、`作废`。

---

### 6.8 激励、互动与宣传

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 积分规则 | P1 | ADM | 学习、组织生活、志愿、双报到等 |
| 积分明细与排行 | P1 | MP | 可按组织范围 |
| 徽章 / 风采 | P2 | MP/ADM | 审核后展示 |
| 双报到积分商城 | P1 | MP/ADM | 在职党员参与社区服务获积分，兑换社区商户优惠或实物奖品 |
| 志愿时长银行 | P1 | MP | 志愿服务时长跨项目累计、跨组织通兑、电子证明一键导出 |
| 微心愿闭环展示墙 | P1 | MP/ADM | 心愿发布→认领→兑现反馈→图文展示墙，形成正向激励传播 |
| 政治生日自动化 | P2 | MP | 自动推送贺卡 + 当年入党照片回忆 + 支部祝福墙留言 |
| 通知公告 / 要闻 | P0 | MP/ADM | 栏目发布 |
| 投票问卷 | P1 | MP | 简易投票、问卷 |
| 议事协商 | P2 | MP/WT | 简版议题与意见收集 |
| 组织生活模板市场 | P1 | WT/ADM | 内置优质主题党日方案模板，支部一键引用并自定义修改 |

---

### 6.9 考核、督办与驾驶舱

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 指标库 | P1 | ADM | 组织生活、学时、党费收缴率、事件时效等 |
| 组织活力指数 | P1 | ADM/BI | 综合多维数据生成可跨组织对比的量化排名指标 |
| 任务考核流程 | P1 | ADM/WT | 下发、填报、佐证、评分 |
| 自动预警 | P1 | 系统 | 未开会、欠费、事件超时、发展超期等 |
| 沉默党员识别 | P1 | ADM/WT | 长期未登录、未参加组织生活、未学习的预警名单自动推送管理员 |
| 活动热度预测 | P2 | ADM/WT | 根据历史报名数据预测场次报名热度，辅助排期决策 |
| 督办单 | P1 | ADM | 生成、催办、销号 |
| 驾驶舱总览 | P1 | BI | 组织、学习、服务、事件核心指标（本租户） |
| 下钻 | P1 | BI | 到支部/网格 |
| AI 自动周报 / 月报 | P1 | MP/ADM | 数据汇总 + AI 生成文字简报，定时推送到书记和管理员手机 |

---

### 6.10 系统管理与安全（租户侧）

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 用户与角色 | P0 | ADM | RBAC |
| 数据权限 | P0 | ADM | 本级 / 本级及下级 / 本网格 / 自定义 |
| 功能开关 | P0 | ADM | 在 OPS 授权模块内再配置 |
| 菜单与组织类型包 | P0 | ADM | 街道包 / 机关包差异化菜单 |
| 字典管理 | P0 | ADM | 系统固定数据维护（组织类型、事件类型、学历等）；支持租户级自定义字典扩展 |
| 参数管理 | P0 | ADM | 系统级 + 租户级两级参数配置；支持热更新无需重启 |
| 操作审计 | P0 | ADM | 登录、关键写操作、导出、AI 调用、开放 API |
| 请求日志 | P0 | 系统 | 中间件自动记录慢查询（>500ms）和异常请求；ADM 可检索排查 |
| Excel 导入导出 | P0 | ADM/WT | 党员名册导入导出、党费批量登记、活动名单导出等场景；支持模板下载和校验报告 |
| 脱敏与金库 | P1 | ADM | 查看完整证件号等需审批 |
| 消息通道配置 | P1 | ADM | 站内信、微信订阅消息、短信 |
| 表单设计器 | P1 | ADM | 拖拽式自定义表单；支撑办事事项引擎 |
| 数据回收站 | P1 | ADM | 所有删除操作走软删除；30 天保留期内可回滚；定期自动清理 |
| 定时任务管理 | P1 | ADM | 在线查看、暂停、恢复、手动触发后台任务（对应工作流 Timer Manager）；含执行日志 |
| SSE 实时推送 | P1 | 全端 | 服务端事件推送替代轮询；用于实时通知、审批状态变更、协同编辑光标同步 |
| 插件化架构 | P1 | ADM/OPS | 功能模块以 WASM 插件形式动态启用禁用；与 SaaS 场景包配合实现按租户功能裁剪 |

---

## 7. AI 能力中台（统一接口）

### 7.1 设计原则

1. 业务服务 **禁止** 直连具体模型厂商 SDK；一律经 **AI Gateway**。
2. 每次调用携带：`tenant_id`、`org_id`、`user_id`、`scene`、`data_level`、`route_policy`、`request_id`。
3. `route_policy`：`local_only` | `cloud_ok` | `auto`（按密级、租户策略、部署模式解析）。
4. 向量库与会话按 **租户隔离**。
5. 全量审计；失败可降级（如仅关键词检索）。
6. Mode-D 可默认 `local_only` 或仅内网模型端点。

### 7.2 能力清单

| 能力编码 | 说明 | 本地 | 云端 | 建议优先级 |
|----------|------|------|------|------------|
| `llm.chat` | 对话 / 公文助手 / 全岗通 | ✓ | ✓ | P1 |
| `doc.summarize` | 摘要 | ✓ | ✓ | P1 |
| `doc.extract` | 要点 / 条款 | ✓ | ✓ | P1 |
| `doc.qa` | 带出处问答 | ✓ | ✓ | P1 |
| `doc.task_breakdown` | 落实清单 | ✓ | ✓ | P1 |
| `ocr.parse` | OCR | ✓ | ✓ | P1 |
| `kb.search` | 知识库检索 | ✓ | ✓ | P0（可先关键词） |
| `face.liveness` | 活体 | ✓优先 | 可选 | P1 |
| `face.compare` | 1:1 比对 | ✓优先 | 可选 | P1 |
| `vision.attention` | 专注度特征 | ✓优先 | 不推荐 | P1（可选功能） |
| `asr.transcribe` | 会议转写 | ✓ | ✓ | P2 |
| `recommend.item` | 课程/活动推荐 | ✓ | ✓ | P2 |
| `moderation.text` | 文本审核 | ✓ | ✓ | P1 |

### 7.3 首期与后期

| 阶段 | 要求 |
|------|------|
| 首期 | Gateway 骨架、云端适配器至少 1 个、审计、路由配置、按租户计量 |
| 后期 | 本地模型与向量库、按密级强制 local_only、信创型号替换 |

### 7.4 高科技 AI 应用场景（V1.2 新增）

以下场景均通过 AI Gateway 统一调用，不依赖硬件设备，全部在小程序 / App / Web 内实现。

#### 7.4.1 AI 公文写作助手

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 公文模板库 | P1 | WT/ADM | 内置通知、总结、汇报、讲话稿等常用模板 |
| 一键生成 | P1 | WT/MP | 输入要点 → LLM 生成完整公文草稿 |
| 润色与改写 | P1 | WT/MP | 对已有文稿进行语气调整、格式规范化 |
| 审计留痕 | P0 | 系统 | 记录 AI 生成操作日志（谁、何时、场景） |

#### 7.4.2 会议纪要自动生成

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 录音上传 / 手机录音 | P1 | MP/WT | 用户上传音频文件或手机端录音 |
| ASR 转写 | P1 | 系统 | 经 AI Gateway 调用语音转文字服务 |
| LLM 结构化纪要 | P1 | 系统 | 自动提取议题、决议、待办事项、参会人 |
| 人工确认与编辑 | P0 | WT | AI 生成的纪要必须经书记/记录人确认后归档 |
| 关联组织生活 | P1 | 系统 | 纪要自动关联对应三会一课记录 |

#### 7.4.3 组织健康度诊断

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 健康度指数 | P1 | ADM/BI | 综合组织生活频率、参与率、学习完成率、党费收缴率等生成可量化指标 |
| 同比环比分析 | P1 | ADM/BI | 与历史数据、同级组织对比 |
| 改进建议 | P1 | ADM/WT | AI 根据薄弱环节自动输出改进建议清单 |
| 定时推送报告 | P1 | ADM/MP | 按月/季度自动推送至书记和管理员 |

#### 7.4.4 党员个人 AI 助手

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 学习规划推荐 | P1 | MP | 根据学时进度和必修缺口推荐课程 |
| 错题分析与巩固 | P1 | MP | 分析薄弱知识点，推送针对性练习 |
| 入党流程答疑 | P1 | MP | 基于知识库回答入党各环节问题 |
| 多轮对话 | P1 | MP | 支持上下文追问，带出处引用 |

#### 7.4.5 舆情与意见分析

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 诉求情感分析 | P2 | ADM/BI | 对群众诉求文本进行情感倾向标注 |
| 热点聚类 | P2 | ADM/BI | 自动聚类高频诉求主题，辅助管理决策 |
| 预警推送 | P2 | ADM | 发现负面情绪集中或敏感关键词时预警 |

#### 7.4.6 AI 数字人讲解员

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| 小程序内虚拟形象 | P2 | MP | 可配置的虚拟讲解员形象与声音 |
| 党务问答 | P2 | MP | 基于知识库回答党建问题 |
| 办事引导 | P2 | MP | 引导用户完成办事流程选择与材料准备 |

### 7.5 沉浸式体验（V1.2 新增）

全部在小程序 / App 内实现，不依赖线下硬件部署。

| 功能点 | 优先级 | 端 | 描述 |
|--------|--------|-----|------|
| VR 全景展馆 | P2 | MP | 360° 全景照片拼接线上展厅；微信内直接浏览；支持热点标注与语音导览 |
| AR 党史打卡 | P2 | MP | 手机摄像头扫描实体海报/场景触发历史影像或故事叠加展示（微信 AR SDK） |
| 交互式场馆导览 | P2 | MP | 小程序内平面图 / 楼层图导航，标注会议室、窗口、功能区位置 |

### 7.6 区块链存证预留（V1.2 新增）

首期仅定义接口与数据模型，不接入真实区块链网络。后续可对接长安链 / FISCO BCOS 等。

| 功能点 | 优先级 | 描述 |
|--------|--------|------|
| 存证中间件接口 | 预留 | 定义 `EvidenceService.submit(hash, bizType, bizId)` 统一入口 |
| 会议记录摘要存证 | 预留 | 三会一课关键记录哈希上链防篡改 |
| 发展党员节点存证 | 预留 | 入党各环节材料摘要上链，跨组织转接时可信校验 |
| 存证查询与验证 | 预留 | 提供存证状态查询与完整性验证 API |

### 7.7 Sunlab 云端 Agent 平台（V1.2 新增，V1.3 细化）

#### 7.7.1 定位

Sunlab 是独立于本项目的云端 Agent 编排平台，负责承载所有 AI Agent 的运行、调度与监控。本项目通过标准 REST / gRPC 接口与 Sunlab 通信，不直接实现 Agent 运行时。

**架构关系：**

```text
本平台业务系统
  └── AI Gateway（统一出口）
        └── Sunlab Agent Platform（云端部署）
              ├── Agent Runtime（各类型 Agent 实例）
              ├── Workflow Orchestrator（多步任务编排）
              ├── Tool Registry（工具注册中心）
              └── Monitoring & Audit（监控审计）
```

#### 7.7.2 Agent 清单

| Agent 名称 | 功能描述 | 触发方式 | 建议优先级 |
|------------|----------|----------|-----------|
| 发展党员跟踪 Agent | 监控各环节时限，到期提醒培养联系人，逾期自动升级上报 | 定时 + 事件触发 | P1 |
| 督办催办 Agent | 根据考核指标发现滞后事项，生成催办通知推送到责任人 | 定时 + 条件触发 | P1 |
| 数据填报 Agent | 从系统抽取数据自动生成报表草稿，人工确认后提交 | 用户发起 | P2 |
| 党务流程自动化 Agent | 解析自然语言指令，自动创建会议、发通知、准备议程等 | 用户发起（对话） | P2 |
| 组织诊断 Agent | 定期分析组织数据，生成健康度报告与改进建议 | 定时触发 | P1 |
| 舆情分析 Agent | 分析诉求文本情感倾向、聚类热点、预警推送 | 事件触发 | P2 |

#### 7.7.3 集成接口约定

本项目需在 AI Gateway 中预留以下接口规范：

```text
# 发起 Agent 任务
POST /ai/agent/execute
{
  "tenant_id": "...",
  "agent_type": "party_dev_tracker",
  "payload": { ... },
  "callback_url": "https://platform.api/callback/agent/{task_id}",
  "priority": "normal",
  "timeout_seconds": 300
}

# 查询任务状态
GET /ai/agent/status/{task_id}

# 接收 Agent 回调（本项目提供）
POST /callback/agent/{task_id}
{
  "status": "completed",
  "result": { ... },
  "audit": { "started_at": "...", "finished_at": "...", "model_used": "..." }
}
```

**安全要求：**
- 本平台与 Sunlab 之间通过 mTLS 或 API Key 认证。
- 所有 Agent 调用必须携带 `tenant_id`，Sunlab 按租户隔离执行环境。
- Agent 输出结果在本平台侧做二次校验后才写入业务库。

#### 7.7.4 部署约定

- Sunlab 支持部署到独立服务器，供多人/多租户使用。
- Mode-S 场景下 Sunlab 可作为共享服务部署在平台侧。
- Mode-D 场景下 Sunlab 可部署在客户内网服务器。
- 具体部署方案由 Sunlab 项目文档定义，本项目仅依赖其 HTTP/gRPC 接口。

#### 7.7.5 各 Agent 详细设计目标

以下设计目标供 sunlab 项目实现时参考；本平台负责提供数据和回调接收。

---

**Agent-01：发展党员跟踪 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 确保每个发展阶段不超期、材料不缺件；将党务工作者从人工盯进度中解放出来 |
| 输入 | 发展党员流程实例数据（当前阶段、开始时间、培养联系人、时限配置） |
| 输出 | 提醒消息 → 推送到本平台消息中心 → 由平台转发给对应人员 |
| 触发 | 每日定时扫描 + 阶段变更事件实时触发 |
| 决策逻辑 | 计算剩余天数 → 分级提醒（提前7天温和提醒 / 到期当天紧急提醒 / 超期自动升级至上级组织员） |
| 依赖工具 | `query_dev_process`（查询发展流程）、`send_notification`（发送通知）、`get_org_hierarchy`（获取组织层级） |
| SLA | 扫描周期 ≤ 24h；事件触发响应 ≤ 5min |
| 异常策略 | 数据缺失时跳过该条并记录日志，不影响其他实例 |

---

**Agent-02：督办催办 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 自动发现考核指标滞后项，生成结构化催办通知，减少管理员手动排查 |
| 输入 | 考核指标当前值 vs 目标值、责任组织/责任人列表、截止时间 |
| 输出 | 催办通知草稿（含指标名称、差距值、建议措施、责任人）→ 平台确认后推送 |
| 触发 | 每周定时扫描 + 指标阈值突破事件触发 |
| 决策逻辑 | 差距计算 → 滞后分级（黄色预警 / 橙色催办 / 红色督办单）→ 根据历史数据推荐改进措施 |
| 依赖工具 | `query_kpi_data`（查询考核数据）、`generate_notice`（生成通知文本）、`get_responsible_persons`（获取责任人） |
| SLA | 扫描周期 ≤ 7d；阈值触发响应 ≤ 30min |
| 异常策略 | 无法确定责任人时上报 ADM 后台待分配 |

---

**Agent-03：数据填报 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 用户用自然语言描述报表需求，Agent 自动从系统抽取数据并生成报表草稿 |
| 输入 | 用户自然语言指令（如「帮我生成本月支部三会一课完成率统计」） |
| 输出 | 结构化报表草稿（表格 + 文字摘要）→ 用户在平台上确认或修改后提交 |
| 触发 | 用户在小程序/App 中发起对话 |
| 决策逻辑 | NLU 解析意图 → 映射到预注册的数据查询模板 → 执行查询 → LLM 生成摘要 → 返回结果 |
| 依赖工具 | `execute_query_template`（执行预注册 SQL 模板）、`format_report`（格式化输出） |
| 安全约束 | 仅允许执行白名单中的查询模板，禁止动态拼接 SQL；结果按租户隔离 |
| SLA | 单次请求响应 ≤ 15s |
| 异常策略 | 意图无法解析时返回引导性提示，不做猜测 |

---

**Agent-04：党务流程自动化 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 通过自然语言对话帮助用户完成多步操作（如「帮我安排下周三下午支部会议，参会人是全体党员」） |
| 输入 | 用户对话上下文 + 当前用户权限与身份 |
| 输出 | 一系列操作计划（创建会议 + 发通知 + 准备议程）→ 用户逐项确认后执行 |
| 触发 | 用户在工作台/小程序中发起对话 |
| 决策逻辑 | 多轮对话管理 → 意图识别 → 操作规划 → 逐项确认 → 调用平台 API 执行 |
| 依赖工具 | `create_meeting`、`send_meeting_notice`、`prepare_agenda`、`query_members` 等（由平台通过 Tool Registry 注册） |
| 安全约束 | 所有写操作必须经过用户确认；权限校验在平台侧完成，Agent 仅做操作编排 |
| SLA | 对话轮次响应 ≤ 3s |
| 异常策略 | 权限不足时明确告知用户原因；操作失败时提供重试或跳过选项 |

---

**Agent-05：组织诊断 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 定期分析组织运行数据，输出健康度评分和可操作的改进建议清单 |
| 输入 | 组织生活频率与参与率、学习完成率、党费收缴率、活动举办次数、发展党员进度等 |
| 输出 | 结构化诊断报告（健康度分数 + 各维度雷达图数据 + Top 3 改进建议）→ 推送至书记和管理员 |
| 触发 | 每月定时触发（可配频率） |
| 决策逻辑 | 多维数据归一化 → 加权计算综合得分 → 与同级组织对比排名 → LLM 生成文字建议 |
| 依赖工具 | `aggregate_org_stats`（聚合统计数据）、`compare_peer_orgs`（同级对比）、`generate_suggestions`（LLM 生成建议） |
| SLA | 报告生成 ≤ 10min |
| 异常策略 | 数据不足的维度标记为「样本量不足」，不计入总分 |

---

**Agent-06：舆情分析 Agent**

| 维度 | 说明 |
|------|------|
| 设计目标 | 对群众诉求和留言进行实时情感分析和主题聚类，辅助管理者发现潜在风险 |
| 输入 | 新提交的诉求文本、公开内容留言文本 |
| 输出 | 情感标签（正面/中性/负面）+ 主题分类 + 置信度；当负面集中度超过阈值时推送预警 |
| 触发 | 新诉求/留言提交事件触发 |
| 决策逻辑 | 文本预处理 → 情感分类 → 主题聚类（增量式）→ 阈值判断 → 生成预警报告 |
| 依赖工具 | `classify_sentiment`、`cluster_topics`、`check_alert_threshold` |
| SLA | 单条分析 ≤ 5s；预警推送 ≤ 1min |
| 异常策略 | 分类置信度低于阈值时标记为「待人工审核」 |

### 7.8 新一代工作流引擎（V1.2 新增）

#### 7.8.1 为什么不用传统 BPMN 引擎

传统方案（Camunda / Activiti / Flowable 等）基于 **命令式 BPMN 流程图**，存在以下问题：

| 问题 | 说明 |
|------|------|
| 硬编码路径 | 所有流程分支必须在 XML 或图中预先画死；新增条件就要改图 |
| 难以动态调整 | 组织架构变化或政策调整时需要重新设计流程图 |
| 与 AI 不兼容 | 传统引擎没有为 AI Agent 决策留出原生扩展点 |
| 状态不可追溯 | 通常只记录当前状态，历史变更难以完整回放 |

#### 7.8.2 技术选型方向

| 技术层 | 方案 | 说明 |
|--------|------|------|
| 事件溯源 | Event Sourcing | 每次审批操作记录为不可变事件流；支持完整回放和审计 |
| 持久化执行 | Durable Execution（参考 Temporal 模式） | 工作流代码化；支持长时间等待人工审批、自动重试、故障恢复 |
| 声明式规则 | Declarative Rules Engine | 用规则约束代替流程图：「什么条件下谁审批」而非「箭头怎么画」 |
| AI Agent 扩展点 | Human-in-the-Loop Signal | 工作流可在任意节点暂停等待人工确认或 Agent 建议；结果作为 Signal 注入继续推进 |

#### 7.8.3 核心概念模型

```text
WorkflowDefinition（工作流定义）
  ├── Rules[]（声明式规则集）
  │     ├── condition: "金额 > 5000 AND org_level >= 2"
  │     ├── approver: "role:org_secretary"
  │     └── sla_hours: 48
  ├── Signals[]（可注入信号）
  │     ├── human.approve / human.reject / human.delegate
  │     ├── agent.suggest（AI Agent 建议）
  │     └── system.timeout_escalate（超时升级）
  └── EventStore（事件溯源存储）

WorkflowInstance（工作流实例）
  ├── current_state: AWAITING_APPROVAL
  ├── history: [Event1, Event2, ...]（不可变）
  └── context: { biz_data, attachments, agent_suggestions }
```

#### 7.8.4 与传统方案的对比优势

| 维度 | 传统 BPMN | 新一代方案 |
|------|-----------|-----------|
| 流程变更 | 改图 → 部署新版本 | 改规则条件 → 即时生效 |
| 动态路由 | 不支持运行时动态改变路径 | 规则引擎实时计算下一节点 |
| AI 集成 | 无原生扩展点 | Agent 可在任何节点注入建议或自动决策 |
| 审计追溯 | 仅日志级别 | 事件溯源天然完整回放 |
| 故障恢复 | 需手动补偿 | Durable Execution 自动恢复到最近一致状态 |
| 长时间等待 | 需外部定时器配合 | 内置持久化计时器，等待数月后自动恢复 |

#### 7.8.5 适用场景映射

| 业务场景 | 工作流模板示例 |
|----------|---------------|
| 三会一课计划审批 | 提交 → 书记审核 → 上级党委备案 |
| 发展党员各阶段审批 | 多级审批 + 时限监控 + Agent 自动催办 |
| 请假审批 | 按天数和职级动态路由审批人 |
| 党费核定审批 | 标准维护 → 复核 → 生效 |
| 网格事件分派 | 分类 → 自动匹配网格员 → 超时自动升级 |
| 办事事项审批 | 自定义表单 + 规则引擎路由 |
| 开放 API 申请 | 提交 → 安全审核 → 租户管理员授权 |

#### 7.8.6 PostgreSQL 数据模型设计

```sql
-- 工作流定义表
CREATE TABLE workflow_definition (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       UUID NOT NULL,
    name            VARCHAR(200) NOT NULL,
    biz_type        VARCHAR(50) NOT NULL,       -- 如 party_dev, meeting_plan, leave_request
    version         INT NOT NULL DEFAULT 1,
    rules           JSONB NOT NULL,              -- 声明式规则集（JSON 格式）
    signals         JSONB NOT NULL DEFAULT '[]', -- 可注入信号类型列表
    status          VARCHAR(20) NOT NULL DEFAULT 'draft', -- draft/active/archived
    created_by      UUID NOT NULL,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 工作流实例表
CREATE TABLE workflow_instance (
    id                  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id           UUID NOT NULL,
    definition_id       UUID NOT NULL REFERENCES workflow_definition(id),
    definition_version  INT NOT NULL,
    biz_type            VARCHAR(50) NOT NULL,
    biz_id              UUID NOT NULL,             -- 关联业务记录 ID
    current_state       VARCHAR(50) NOT NULL,      -- 如 submitted, awaiting_approval, completed
    context             JSONB NOT NULL DEFAULT '{}', -- 业务上下文快照
    initiated_by        UUID NOT NULL,
    started_at          TIMESTAMPTZ NOT NULL DEFAULT now(),
    completed_at        TIMESTAMPTZ,
    expires_at          TIMESTAMPTZ                -- SLA 到期时间
);

-- 事件存储表（Event Sourcing 核心）
CREATE TABLE workflow_event (
    id              BIGSERIAL PRIMARY KEY,
    instance_id     UUID NOT NULL REFERENCES workflow_instance(id),
    tenant_id       UUID NOT NULL,
    event_type      VARCHAR(50) NOT NULL,        -- SUBMITTED, APPROVED, REJECTED, DELEGATED, AGENT_SUGGESTED, TIMEOUT_ESCALATED...
    actor_id        UUID,                        -- 操作人（系统事件为 NULL）
    actor_type      VARCHAR(20) NOT NULL DEFAULT 'human', -- human/system/agent
    payload         JSONB NOT NULL DEFAULT '{}', -- 事件详情（意见、附件等）
    occurred_at     TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_wf_event_instance ON workflow_event(instance_id, occurred_at);
CREATE INDEX idx_wf_event_tenant ON workflow_event(tenant_id);

-- 审批规则表（声明式规则集，从 definition.rules 展开便于查询）
CREATE TABLE workflow_rule (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id       UUID NOT NULL,
    definition_id   UUID NOT NULL REFERENCES workflow_definition(id),
    rule_order      INT NOT NULL,               -- 优先级顺序
    condition_expr  TEXT NOT NULL,              -- 条件表达式（DSL 格式）
    approver_expr   TEXT NOT NULL,              -- 审批人表达式（如 "role:org_secretary" 或 "user:${initiated_by.supervisor}"）
    sla_hours       INT,                        -- 审批时限（小时），NULL 表示不限
    escalation_rule TEXT,                        -- 超时升级规则（JSON）
    is_active       BOOLEAN NOT NULL DEFAULT TRUE
);

-- 信号注入表（Human-in-the-Loop / Agent 建议）
CREATE TABLE workflow_signal (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    instance_id     UUID NOT NULL REFERENCES workflow_instance(id),
    tenant_id       UUID NOT NULL,
    signal_type     VARCHAR(50) NOT NULL,        -- human.approve, human.reject, agent.suggest, system.timeout_escalate
    sender_id       UUID,                        -- 发送者（human 或 agent ID）
    sender_type     VARCHAR(20) NOT NULL,        -- human/system/agent
    data            JSONB NOT NULL DEFAULT '{}', -- 信号载荷（审批意见、Agent 建议内容等）
    consumed        BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    consumed_at     TIMESTAMPTZ
);

-- 审批任务表（当前等待某人操作的节点）
CREATE TABLE workflow_task (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    instance_id     UUID NOT NULL REFERENCES workflow_instance(id),
    tenant_id       UUID NOT NULL,
    assignee_id     UUID NOT NULL,
    assignee_type   VARCHAR(20) NOT NULL DEFAULT 'user', -- user/role/org
    status          VARCHAR(20) NOT NULL DEFAULT 'pending', -- pending/completed/expired/cancelled
    sla_deadline    TIMESTAMPTZ,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    completed_at    TIMESTAMPTZ
);
```

#### 7.8.7 条件表达式 DSL 设计

条件表达式采用安全子集语法，不支持任意代码执行：

```text
# 示例 1：金额超过 5000 且发起人为科级以上
amount > 5000 AND initiator.rank_level >= 2

# 示例 2：请假天数 > 3 天需要上级党委审批
days > 3 THEN role:org_committee

# 示例 3：网格事件按类型路由
event_type == "sanitation" THEN org:grid_worker_team_1
event_type == "security"   THEN org:grid_worker_team_2

# 示例 4：组合条件
(org.type == "street" AND member_count > 50) OR is_pilot_org == true
THEN role:district_committee_member
```

**支持的运算符：**
- 比较：`==`, `!=`, `>`, `<`, `>=`, `<=`
- 逻辑：`AND`, `OR`, `NOT`
- 存在性：`EXISTS(field)`
- 字符串匹配：`field CONTAINS "keyword"`

**表达式求值引擎选型建议：**
- Go 项目：[expr-lang/expr](https://github.com/expr-lang/expr) 或 [google/cel-go](https://github.com/google/cel-go)
- Java 项目：Easy-Rules 或 SpEL 受限模式
- Rust 项目：[evalexpr](https://github.com/ISibboI/evalexpr)

#### 7.8.8 工作流引擎核心接口

```text
# 创建工作流定义
POST   /api/v1/workflows/definitions

# 启动工作流实例
POST   /api/v1/workflows/instances
{
  "definition_name": "meeting_plan_approval",
  "biz_type": "meeting_plan",
  "biz_id": "...",
  "context": { ... }
}

# 注入信号（人工审批 / Agent 建议 / 系统超时）
POST   /api/v1/workflows/instances/{id}/signals
{
  "signal_type": "human.approve",
  "sender_id": "...",
  "data": { "comment": "同意", "attachments": [] }
}

# 查询实例状态与完整历史
GET    /api/v1/workflows/instances/{id}

# 查询我的待办任务
GET    /api/v1/workflows/tasks?assignee_id={current_user_id}&status=pending

# 回放实例完整事件流（审计用）
GET    /api/v1/workflows/instances/{id}/events
```

#### 7.8.9 Durable Execution 实现

参考 Temporal 的持久化执行模式，但可以轻量化实现：

| 组件 | 职责 | 实现方式 |
|------|------|----------|
| Workflow Runner | 执行工作流逻辑函数 | 状态机循环：读取当前状态 → 匹配规则 → 创建任务或推进状态 |
| Signal Handler | 监听并消费注入的信号 | 数据库轮询（简单）或消息队列订阅（高吞吐） |
| Timer Manager | 管理 SLA 计时器与超时升级 | PostgreSQL `pg_cron` 或独立调度器扫描 `workflow_task.sla_deadline` |
| Recovery Worker | 故障恢复 | 应用重启后从 `workflow_event` 表回放到最新一致状态 |

**轻量化方案（首期推荐）：**

不引入 Temporal 服务端，而是用 PostgreSQL + 应用层状态机实现同等效果：

```text
应用启动
  └── Recovery: SELECT instances WHERE current_state != 'completed'
      └── 对每个未完成实例：从 event 表重建上下文 → 恢复计时器

定时器扫描
  └── pg_cron 每 60s: SELECT tasks WHERE sla_deadline < now() AND status = 'pending'
      └── 注入 system.timeout_escalate 信号

信号消费
  └── 应用内 goroutine/consumer 轮询 workflow_signal WHERE consumed = false
      └── 按 FIFO 顺序消费 → 推进状态机 → 写入新事件
```

**优势：** 不增加额外基础设施依赖（不需要部署 Temporal Server）；PostgreSQL 已是必选项；Mode-D 场景更容易交付。

**升级路径：** 当并发量和复杂度增长后，可平滑迁移到 Temporal 或自研持久化执行框架，业务层接口不变。

---

## 8. 开放平台（网格及其他）

### 8.1 认证

- `AppId` + `AppSecret` 获取 `access_token`（**绑定 tenant_id**）
- 请求签名（时间戳 + nonce + body 摘要）
- IP 白名单可选
- 权限范围（scope）：事件读写、只读统计等
- 受租户配额与 QPS 限制

### 8.2 网格事件 API（逻辑清单）

| 方法 | 路径（示例） | 说明 |
|------|----------------|------|
| POST | `/open/v1/grid/events` | 创建事件 |
| PATCH | `/open/v1/grid/events/{id}` | 更新状态 / 回写结果 |
| GET | `/open/v1/grid/events/{id}` | 查询详情 |
| GET | `/open/v1/grid/events` | 分页查询 |
| POST | `/open/v1/grid/webhooks` | 注册回调地址 |
| POST | `/open/v1/grid/events/{id}/comments` | 补充办理意见 |

### 8.3 其他预留开放能力

- 组织 / 党员主数据只读同步（P2）
- 活动只读日历（P2）
- 学习完成结果回传（P2）

---

## 9. 支付适配层（本期不做业务，必须预留）

### 9.1 本期范围

- **不实现**：微信 / 支付宝等真实下单、用户扣款、支付回调业务逻辑上线。
- **必须实现或设计落位**：
  - 支付通道配置表（通道编码、商户号占位、启用状态、**tenant_id**）
  - 党费账单与「支付单」关联模型
  - 统一支付服务接口（空实现 / Mock）
  - 对账文件导入与差异表结构（可空跑）
  - 管理端入口可隐藏，代码层插件可注册

### 9.2 建议接口（逻辑）

```text
PaymentService.createOrder(PayOrderCreateCmd) -> PayOrderDTO
PaymentService.queryOrder(orderNo) -> PayOrderDTO
PaymentService.handleNotify(channel, headers, body) -> NotifyResult
PaymentService.closeOrder(orderNo) -> void
PaymentService.refund(RefundCmd) -> RefundDTO   // 预留，党费场景慎用
```

### 9.3 数据对象（建议字段）

**pay_channel**：tenant_id、channel_code、name、enabled、merchant_no、ext_json  

**pay_order**：tenant_id、order_no、biz_type（PARTY_DUES）、biz_id、amount、status  
（INIT/SUCCESS/FAIL/CLOSED）、channel_code、channel_trade_no、paid_at、raw_notify（加密存储）  

**dues_bill**：与支付单关联；本期仅走 `OFFLINE_REGISTERED`。

### 9.4 账单状态机（含未来支付）

```text
PENDING（待缴）
  ├─ OFFLINE_REGISTERED（线下已登记）    ← 本期主路径
  ├─ PAYING（支付中）                    ← 预留
  ├─ PAID（已支付）                      ← 预留
  ├─ OVERDUE（逾期）
  └─ CANCELLED（作废）
```

### 9.5 验收要点（支付预留）

1. 业务代码通过接口依赖支付能力，无直接耦合某支付 SDK。
2. 关闭所有支付通道时，党费线下登记全流程不受影响。
3. 开启 Mock 通道时可完成「创建订单 → 模拟成功 → 账单 PAID」联调（仅测试环境）。
4. 文档与配置中明确：上线真实支付前需完成商户主体、党费专户归集与合规评估。

---

## 10. 非功能需求

### 10.1 安全（对标等保二级及以上）

- 身份鉴别、访问控制、安全审计、通信完整性与保密性（TLS）
- **租户隔离**纳入安全设计与测试范围
- 重要个人敏感信息存储加密；密钥与配置分离
- 防 SQL 注入、XSS、CSRF、越权（水平/垂直/**跨租户**）
- 会话超时、登录失败锁定、验证码策略
- 人脸与视觉数据：最小化、授权、可删除、审计；归属租户
- 开放 API：限流、签名、审计

### 10.2 数据与存储

- 主数据库：**PostgreSQL**
- Mode-S：共享实例 + `tenant_id`；Mode-D：独立实例/独立库
- 文件：对象存储（私有桶）；路径含租户；证书与人脸特征分策略存储
- 备份恢复策略与定期演练记录（运维）；独立部署备份归客户侧约定

### 10.3 性能与可用性（建议指标，实施时可调整）

- 核心查询接口 P95 < 500ms（常规数据量）
- 签到高峰支持活动级并发（按部署规模压测）
- 标准版支持租户级限流，避免单一租户挤占集群

### 10.4 合规与隐私

- 隐私政策与授权文案（人脸、定位、摄像头）
- 日志脱敏
- 数据导出审批；租户关闭与数据销毁流程

### 10.5 多组织与可配置

- 功能开关、流程节点、表单字段、积分与学时规则均可配置
- 街道场景包 / 机关场景包可默认启用不同菜单
- 开关受 **平台套餐 ∩ 租户配置** 双重约束

### 10.6 可运维性（SaaS）

- 配置中心区分平台级 / 租户级
- 版本号可查询；独立部署支持离线升级包
- 关键指标按租户可观测（Mode-S）

---

## 11. 信息架构（菜单建议）

### 11.1 党员端（MP）

- 首页（待办、公告、学习进度、活动）
- 学习（课程、文件、考试）
- 组织生活（会议、签到、请假）
- 服务（活动、办事、诉求、微心愿、场地）
- 我的（档案、党费记录、积分、消息、设置；可切换租户若多隶属）

### 11.2 群众端（MP）

- 首页（公告、活动、办事入口）
- 活动
- 办事
- 诉求 / 微心愿
- 我的

> 党员与群众可通过同一小程序按身份切换；无党员身份不展示党务敏感菜单。

### 11.3 党务工作台（WT）

- 待办中心
- 本组织党员与发展
- 组织生活
- 活动与场地
- 办事受理
- 诉求 / 事件
- 学习管理（本级）
- 数据简报

### 11.4 租户管理后台（ADM）

- 组织与权限
- 党员与发展
- 党费（标准、登记、统计）
- 组织生活与公开
- 党群与事项配置
- 学习与题库
- 网格与开放应用
- 考核督办
- 内容与门户
- 系统配置（开关、消息、AI 路由、人脸场景）
- 审计日志
- 本租户配额使用情况（只读）

### 11.5 平台运营后台（OPS）

- 租户管理（开通、冻结、关闭）
- 套餐与功能包
- 配额与用量
- 全局监控与告警
- 独立实例台账
- 平台审计
- 运营公告（P2）

### 11.6 驾驶舱（BI）

- 总览、组织生活、学习、服务办件、网格事件、预警列表（数据范围 = 当前租户授权组织）

---

## 12. 分期计划

### 12.1 MVP（P0 为主）

**SaaS 底座**

- Tenant 模型 + 全局 `tenant_id` 拦截
- OPS：开户 / 冻结 / 场景包 / 基础配额
- 开通向导；Mode-S 可运行
- Mode-D 部署参数与安装说明（可先手工）
- 跨租户越权测试门禁

**业务**

- 多组织 + 权限（租户内）
- 字典管理 + 参数管理 + 请求日志
- Excel 导入导出（党员名册、党费批量登记）
- 党员档案、党费标准与线下登记统计
- 三会一课全流程 + 扫码/定位签到
- 文件/视频学习 + 停留与有效观看 + 学时
- 活动报名与场地预约
- 办事事项引擎（基础）
- 诉求上报办理
- 网格事件内部闭环
- 租户 ADM 与审计基础
- PostgreSQL 落地
- 支付适配层空实现 + 数据模型
- AI Gateway 骨架（可先不接业务）

### 12.2 V1（P1）

- 发展党员细化、转接
- 人脸签到/考试（可配）、补签审批
- 区域化党建与双报到
- 知识库 + OCR/摘要/问答（云端）
- AI 公文写作助手
- 会议纪要自动生成（录音上传 → ASR → LLM）
- 组织健康度诊断报告
- 党员个人 AI 助手（学习规划 + 错题分析）
- AI 自动周报 / 月报
- Sunlab Agent：发展党员跟踪 + 督办催办 + 组织诊断（P1 Agent 优先接入）
- 新一代工作流引擎：事件溯源 + 声明式规则 + 持久化执行
- 审批流可视化设计器（拖拽配置规则条件）
- 图片 OCR + 语义理解（拍照上传 → 结构化入库）
- 协同文档编辑（会议纪要多人在编辑场景）
- 网格开放 API（租户绑定）
- 考核指标与预警 + 沉默党员识别 + 组织活力指数
- 视觉专注度可选
- 积分 + 双报到积分商城 + 志愿时长银行 + 微心愿闭环展示墙
- 组织生活模板市场
- 学习小组（打卡排行 + 讨论区）
- 租户品牌基础、用量看板
- 独立部署交付件完善

### 12.3 V2（P2 及强化）

- 本地大模型与向量库
- 驾驶舱完善 + 活动热度预测
- VR 全景展馆 / AR 党史打卡 / 交互式场馆导览
- AI 数字人讲解员
- 舆情与意见分析
- 区块链存证落地（对接真实链）
- Sunlab Agent：数据填报 + 党务流程自动化 + 舆情分析（P2 Agent 接入）
- 知识图谱（党规党纪关联网络 + 组织关系图谱 + 图谱问答）
- 视频内容摘要与章节标记
- 图表智能解读（驾驶舱 AI 文字解读）
- 语音交互（语音输入 → AI 理解 → 跳转功能）
- 报表自定义设计器
- 自动化规则引擎（If-This-Then-That 业务规则）
- 联邦学习预留 / 差分隐私统计 / 零知识证明身份验证（预留接口）
- 党费使用台账、工资基数对接
- 真实支付通道接入（合规评估后）
- 计费/订阅对接、工单
- 上级平台或跨租户数据交换（慎用）

---

## 13. 验收总则

1. **功能验收**：按优先级用例；P0 全部通过方可上线主场景。
2. **权限验收**：跨组织越权必须失败并审计。
3. **租户验收**：跨租户越权必须失败并审计；见第 3.9 节。
4. **部署验收**：同一制品完成 Mode-S 与 Mode-D 冒烟。
5. **安全验收**：对照等保二级控制点清单（可分阶段整改）。
6. **支付预留验收**：见第 9.5 节。
7. **开放能力验收**：外部模拟应用完成事件创建与状态回写（限定本租户）。
8. **AI 验收**：同一业务在 `cloud_ok` 与 Mock `local` 下路由可切换；审计含 tenant_id。
9. **隐私验收**：关闭人脸/专注度后相关入口不可用且无残留强制逻辑。

---

## 14. 附录

### 14.1 术语

| 术语 | 说明 |
|------|------|
| 租户 Tenant | SaaS 签约与数据隔离的基本单位（街道/机关/集团等） |
| 标准版多租户 Mode-S | 共享集群，行级 tenant_id 隔离 |
| 独立部署 Mode-D | 大客户专有实例与独立数据面 |
| 平台运营 OPS | 平台方管理租户与实例的后台 |
| 党群服务中心 | 街道/社区服务党员群众的综合性阵地 |
| 机关党委 | 机关党的基层委员会，抓机关党建专责 |
| 三会一课 | 支部党员大会、支委会、党小组会和党课 |
| 双报到 | 在职党员到社区报到并参与服务 |
| AI Gateway | 统一 AI 调用网关 |
| 支付适配层 | 屏蔽具体支付渠道的领域服务（本期空实现） |

### 14.2 关联文档（建议后续补充）

- 《接口设计说明书》（含 OPEN 与 Payment 详细字段）
- 《数据模型说明书》（PostgreSQL，含 tenant_id 规范）
- 《SaaS 部署手册》（Mode-S / Mode-D）
- 《等保二级安全设计方案》
- 《AI 路由与数据分级策略》
- 《UI 信息架构与原型》

### 14.3 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-08-24 | 首版：完全体功能基线；党费支付不做但预留接口；网格自建闭环并开放 |
| V1.1 | 2026-08-24 | 增加 SaaS：**标准版多租户 + 大客户独立部署**；租户模型、OPS、配额、双模式部署与验收；全文统一租户上下文 |
| V1.2 | 2026-08-24 | 增加：AI 高科技应用场景（公文助手 / 会议纪要 / 组织诊断 / 个人助手 / 舆情分析 / 数字人）；沉浸式体验（VR 全景展馆 / AR 打卡 / 场馆导览）；区块链存证预留；激励丰富化（积分商城 / 时长银行 / 微心愿展示墙 / 模板市场）；数据智能（活力指数 / 沉默党员识别 / 热度预测 / AI 周报）；明确不做 IoT 硬件类功能 |
| V1.3 | 2026-08-24 | 增加：Sunlab 云端 Agent 平台专节（定位 / Agent 清单 / 集成接口 / 部署约定）；新一代工作流引擎（事件溯源 + 声明式规则 + 持久化执行 + AI Agent 扩展点，替代传统 BPMN）；V1 新增协同编辑 / 审批设计器 / OCR 语义理解 / 学习小组；V2 新增知识图谱 / 语音交互 / 报表设计器 / 自动化规则引擎 / 隐私计算预留；去掉直播功能 |
| V1.3a | 2026-08-24 | 增加：技术栈选型专节（3.10：后端全栈 Rust / WASM 沙箱 / 移动端 uni-app x 基于 Cool Unix 开源脚手架二次开发 / 管理端 Vue 3 + shadcn-vue + Tailwind CSS）；系统管理补齐 cool-admin 参考功能；MVP 补充字典参数和 Excel 导入导出 |
| V1.4 | 2026-08-24 | 新增 TA-08 SDUI 动态渲染引擎详细设计：Schema 协议、声明式 Action 管线、数据聚合、Diff 增量更新、三层缓存、A/B 灰度发布、可视化编辑器协议 |

---

**文档结束**
