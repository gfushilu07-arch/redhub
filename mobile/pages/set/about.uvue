<template>
	<mx-page>
		<view class="p-3">
			<view class="flex flex-col items-center justify-center py-10">
				<view class="p-4 bg-white rounded-2xl">
					<mx-image src="/static/logo.png" :height="60" :width="60"></mx-image>
				</view>
				<mx-text :size="16" :pt="{ className: 'mt-4 mb-1 font-bold' }">{{
					config.name
				}}</mx-text>
			</view>

			<mx-list>
				<mx-list-item
					:label="t('访问官网')"
					arrow
					hoverable
					@tap="toWebSite"
				></mx-list-item>
			</mx-list>
		</view>
	</mx-page>
</template>

<script setup lang="ts">
import { config } from "@/config";
import { $t, t } from "@/.mx";
import { openWeb } from "@/uni_modules/mx-open-web";

onReady(() => {
	uni.setNavigationBarTitle({
		title: $t("关于{name}", { name: config.name })
	});
});

function toWebSite() {
	openWeb(config.website);
}
</script>
