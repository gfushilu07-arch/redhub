<template>
	<mx-page>
		<view class="p-3">
			<view class="flex flex-col justify-center items-center py-10 mb-3">
				<view class="relative overflow-visible">
					<!-- #ifdef MP-WEIXIN -->
					<button
						class="absolute top-0 left-0 w-full h-full opacity-0 z-10"
						open-type="chooseAvatar"
						@chooseavatar="uploadAvatar"
					></button>
					<!-- #endif -->

					<mx-avatar
						:src="userInfo?.avatarUrl"
						:size="80"
						:pt="{ className: '!rounded-3xl', icon: { size: 32 } }"
						@tap="uploadAvatar"
					>
					</mx-avatar>

					<view
						class="flex flex-col justify-center items-center absolute bottom-0 right-[-3px] bg-black rounded-full p-1 border border-solid border-white"
					>
						<mx-icon name="edit-line" color="white" :size="10"></mx-icon>
					</view>
				</view>
			</view>

			<mx-list :pt="{ className: 'mb-3' }">
				<mx-list-item
					:label="t('我的昵称')"
					hoverable
					arrow
					justify="start"
					@tap="router.to('/pages/user/edit-name')"
				>
					<mx-text>{{ userInfo?.nickName }}</mx-text>
				</mx-list-item>
				<mx-list-item label="手机号" hoverable justify="start">
					<mx-text>{{ userInfo?.phone }}</mx-text>
				</mx-list-item>
			</mx-list>

			<mx-list :pt="{ className: 'mb-3' }">
				<mx-list-item
					:label="t('简介')"
					hoverable
					arrow
					justify="start"
					@tap="router.to('/pages/user/edit-description')"
				>
					<mx-text color="info" v-if="userInfo?.description == null">{{
						t("介绍一下自己")
					}}</mx-text>
					<mx-text ellipsis v-else>{{ userInfo?.description }}</mx-text>
				</mx-list-item>
			</mx-list>

			<mx-list :pt="{ className: 'mb-3' }">
				<mx-list-item
					:label="t('性别')"
					hoverable
					arrow
					justify="start"
					@tap="open('gender')"
				>
					<mx-text>{{ genderText }}</mx-text>
					<mx-text color="info" v-if="genderText == ''">{{ t("编辑性别") }}</mx-text>
				</mx-list-item>

				<mx-list-item
					:label="t('生日')"
					hoverable
					arrow
					justify="start"
					@tap="open('birthday')"
				>
					<mx-text>{{ userInfo?.birthday }}</mx-text>
					<mx-text color="info" v-if="userInfo?.birthday == null">{{
						t("选择生日")
					}}</mx-text>
				</mx-list-item>

				<mx-list-item
					:label="t('地区')"
					hoverable
					arrow
					justify="start"
					@tap="open('region')"
				>
					<mx-text>{{ regionText }}</mx-text>
					<mx-text color="info" v-if="regionText == ''">{{
						t("选择所在的地区")
					}}</mx-text>
				</mx-list-item>
			</mx-list>

			<mx-select
				:title="t('选择性别')"
				:model-value="userInfo?.gender"
				:ref="refs.set('gender')"
				:options="genderOptions"
				:show-trigger="false"
				@change="onGenderChange"
			></mx-select>

			<mx-select-date
				:title="t('选择生日')"
				:model-value="userInfo?.birthday"
				:ref="refs.set('birthday')"
				type="date"
				:end="today"
				:show-trigger="false"
				@change="onBirthdayChange"
			></mx-select-date>

			<mx-cascader
				:title="t('选择所在的地区')"
				:ref="refs.set('region')"
				:options="regionOptions"
				:show-trigger="false"
				@change="onRegionChange"
			></mx-cascader>
		</view>
	</mx-page>
</template>

<script setup lang="ts">
import { dayUts, router, upload, useRefs, useStore, type Response, userInfo, t } from "@/.mx";
import { useCascader, useUi, type MxSelectOption } from "@/uni_modules/mx-ui";
import { computed, ref } from "vue";
import pca from "@/.mx/data/pca.json";

const { user } = useStore();
const ui = useUi();
const refs = useRefs();

// 今天
const today = dayUts().format("YYYY-MM-DD");

// 性别选项
const genderOptions = ref<MxSelectOption[]>([
	{
		label: t("保密"),
		value: 0
	},
	{
		label: t("男"),
		value: 1
	},
	{
		label: t("女"),
		value: 2
	}
]);

// 性别文本
const genderText = computed(() => {
	return [t("保密"), t("男"), t("女")][userInfo.value?.gender!];
});

// 性别改变
function onGenderChange(val: number) {
	user.update({
		gender: val
	});

	ui.showToast({
		message: t("性别设置成功")
	});
}

// 生日改变
function onBirthdayChange(val: string) {
	user.update({
		birthday: val
	});

	ui.showToast({
		message: t("生日设置成功")
	});
}

// 地区选项
const regionOptions = useCascader(pca);

// 地区文本
const regionText = computed(() => {
	return [userInfo.value?.province, userInfo.value?.city, userInfo.value?.district]
		.filter((e) => e != null)
		.join(" - ");
});

// 地区改变
function onRegionChange(arr: string[]) {
	user.update({
		province: arr[0],
		city: arr[1],
		district: arr[2]
	});

	ui.showToast({
		message: t("地区设置成功")
	});
}

// 打开弹窗
function open(name: string) {
	refs.open(name);
}

// 上传头像
function uploadAvatar(e: UniEvent) {
	function next(path: string) {
		upload(path)
			.then((url) => {
				ui.showToast({
					message: t("头像上传成功")
				});

				user.update({
					avatarUrl: url
				});
			})
			.catch((err) => {
				ui.showToast({
					message: (err as Response).message!
				});
			});
	}

	// #ifdef MP-WEIXIN
	next(e.detail.avatarUrl);
	// #endif

	// #ifndef MP-WEIXIN
	uni.chooseImage({
		count: 1,
		success(res) {
			next(res.tempFiles[0].path);
		}
	});
	// #endif
}
</script>
