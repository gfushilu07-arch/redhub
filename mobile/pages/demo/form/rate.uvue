<template>
	<mx-page>
		<view class="p-3">
			<demo-item :label="t('基础用法')">
				<mx-rate v-model="num"></mx-rate>
			</demo-item>

			<demo-item :label="t('自定义')">
				<mx-rate
					v-model="num2"
					:disabled="isDisabled"
					:show-score="isShowScore"
					:allow-half="isAllowHalf"
					:size="isSize ? 24 : 20"
					:void-icon="isIcon ? 'heart-fill' : 'star-fill'"
					:icon="isIcon ? 'heart-fill' : 'star-fill'"
				></mx-rate>

				<mx-list
					border
					:pt="{
						className: 'mt-3'
					}"
				>
					<mx-list-item :label="t('只读')">
						<mx-switch v-model="isDisabled"></mx-switch>
					</mx-list-item>

					<mx-list-item :label="t('显示分数')">
						<mx-switch v-model="isShowScore"></mx-switch>
					</mx-list-item>

					<mx-list-item :label="t('允许半星')">
						<mx-switch v-model="isAllowHalf"></mx-switch>
					</mx-list-item>

					<mx-list-item :label="t('换个图标')">
						<mx-switch v-model="isIcon"></mx-switch>
					</mx-list-item>

					<mx-list-item :label="t('大一点')">
						<mx-switch v-model="isSize"></mx-switch>
					</mx-list-item>
				</mx-list>
			</demo-item>
		</view>
	</mx-page>
</template>

<script lang="ts" setup>
import { ref } from "vue";
import DemoItem from "../components/item.uvue";
import { t } from "@/.mx";

const num = ref(1);
const num2 = ref(3.5);
const isDisabled = ref(false);
const isShowScore = ref(true);
const isAllowHalf = ref(true);
const isSize = ref(false);
const isIcon = ref(false);
</script>
