<template>
	<mx-page>
		<view class="p-4">
			<mx-section title="操作控件" description="统一触控尺寸、动作层级和异步反馈。">
				<view class="flex flex-row overflow-visible">
					<mx-button fluid>提交审批</mx-button>
					<mx-button type="info" text border fluid>暂存草稿</mx-button>
				</view>

				<view class="flex flex-row mt-3 overflow-visible">
					<mx-button type="error" text border fluid>驳回</mx-button>
					<mx-button loading fluid>处理中</mx-button>
					<mx-button disabled fluid>不可用</mx-button>
				</view>
			</mx-section>

			<mx-section title="表单状态" description="默认、聚焦、禁用和错误状态保持相同布局。">
				<view class="demo-field">
					<mx-text :size="13" :pt="{ className: 'mb-2 font-bold' }">组织名称</mx-text>
					<mx-input
						v-model="organizationName"
						prefix-icon="building-2-line"
						placeholder="请输入组织名称"
						clearable
					></mx-input>
				</view>

				<view class="demo-field">
					<mx-text :size="13" :pt="{ className: 'mb-2 font-bold' }">联系电话</mx-text>
					<mx-input model-value="138 0000 0000" disabled></mx-input>
				</view>

				<view class="demo-field">
					<mx-text :size="13" :pt="{ className: 'mb-2 font-bold' }">审批标题</mx-text>
					<mx-input v-model="approvalTitle" invalid placeholder="请输入审批标题"></mx-input>
					<mx-text color="error" :size="12" :pt="{ className: 'mt-2' }">审批标题不能为空</mx-text>
				</view>

				<view class="demo-field">
					<mx-text :size="13" :pt="{ className: 'mb-2 font-bold' }">申请说明</mx-text>
					<mx-textarea
						v-model="description"
						placeholder="说明事项背景、依据和期望完成时间"
						:maxlength="120"
					></mx-textarea>
				</view>
			</mx-section>

			<mx-section title="状态语义" description="颜色与文字同时表达状态，避免只靠色相识别。">
				<view class="demo-status-row">
					<mx-tag type="primary">重点</mx-tag>
					<mx-tag type="success">已通过</mx-tag>
					<mx-tag type="warn">待审核</mx-tag>
					<mx-tag type="error">已驳回</mx-tag>
					<mx-tag type="info">草稿</mx-tag>
				</view>

				<view class="demo-status-row mt-3">
					<mx-tag type="primary" plain>正式党员</mx-tag>
					<mx-tag type="warn" plain closable>筛选条件</mx-tag>
				</view>

				<view class="flex flex-row mt-3 overflow-visible">
					<mx-button type="info" text border>
						通知
						<template #content>
							<mx-badge type="error" value="12" position></mx-badge>
						</template>
					</mx-button>
					<mx-button type="info" text border>
						新动态
						<template #content>
							<mx-badge type="primary" dot position></mx-badge>
						</template>
					</mx-button>
				</view>
			</mx-section>

			<mx-section title="页面区块" description="统一 16px 页边距、24px 区块间距和 14px 卡片圆角。">
				<template #action>
					<mx-tag rounded>4px Grid</mx-tag>
				</template>

				<mx-card title="组织活动" subtitle="近 30 天" shadow>
					<mx-text :size="13" color="info">使用 Card 承载同一段业务信息，避免多个容器样式漂移。</mx-text>
					<template #footer>
						<mx-button size="small" text>查看详情</mx-button>
					</template>
				</mx-card>
			</mx-section>

			<mx-section title="指标展示" description="用于首页、驾驶舱和个人中心的重点数据。">
				<mx-row :gutter="10">
					<mx-col :span="6">
						<mx-metric label="党员人数" value="1286" trend="+3.2%" trend-type="up" icon="group-line"></mx-metric>
					</mx-col>
					<mx-col :span="6">
						<mx-metric label="待办事项" value="18" trend="-2件" trend-type="down" icon="file-copy-line"></mx-metric>
					</mx-col>
				</mx-row>
			</mx-section>
		</view>
	</mx-page>
</template>

<script setup lang="ts">
import { ref } from "vue";

const organizationName = ref("红星社区党委");
const approvalTitle = ref("");
const description = ref("");
</script>

<style lang="scss" scoped>
.demo-field {
	margin-bottom: 16px;
}

.demo-status-row {
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;
	align-items: center;
	min-height: 44px;
	overflow: visible;
}
</style>
