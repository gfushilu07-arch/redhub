<template>
	<mx-page>
		<view class="p-3">
			<demo-item :label="t('跳转')">
				<mx-button @click="toPush">{{ t("跳转") }}</mx-button>
			</demo-item>

			<demo-item :label="t('带参数')">
				<mx-button @click="toQuery">{{ t("跳转") }}</mx-button>
			</demo-item>

			<demo-item :label="t('需登录')">
				<mx-button @click="toLogin">{{ t("跳转") }}</mx-button>
			</demo-item>
		</view>
	</mx-page>
</template>

<script lang="ts" setup>
import { router, uuid } from "@/.mx";
import DemoItem from "../../components/item.uvue";
import { t } from "@/.mx";

function toPush() {
	router.to("/pages/demo/other/router/query");
}

function toQuery() {
	router.push({ path: "/pages/demo/other/router/query", query: { id: uuid() } });
}

function toLogin() {
	router.push({
		path: "/pages/demo/other/router/query",
		query: { id: uuid() },
		isAuth: true
	});
}
</script>

<style lang="scss" scoped></style>
