[build]
# Use project-specific target dir (overridden by CARGO_TARGET_DIR env if set)
target-dir = "/Volumes/fushilu/.caches/redhub/cargo-target"
jobs = 8

[registries.crates-io]
protocol = "sparse"

[net]
git-fetch-with-cli = true

[alias]
dev = "run -p redhub-server"
